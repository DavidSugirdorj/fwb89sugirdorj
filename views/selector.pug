extends layout

block content
  h1= title
  - let image_names = ["aang.png", "Katara.png", "sokka.png", "tylee.png", "zuko.png"];
    - for(let i=0; i<3; i++)
      - let rand = Math.floor(Math.random()*5);
        img(height='300' width='300' src=`images/${image_names[rand]}` alt='#{image_names[rand]}')  



